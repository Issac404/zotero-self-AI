// Auto-generated by scripts/gen-env-setup.mjs — DO NOT EDIT
// Preload environment-based preferences for tests
(() => {
  try {
    // provider
    Zotero?.Prefs?.set?.("extensions.zotero.selfAI.provider", "google", true);
    // stream
    Zotero?.Prefs?.set?.("extensions.zotero.selfAI.stream", true, true);
    // temperature
    Zotero?.Prefs?.set?.("extensions.zotero.selfAI.temperature", "0.7", true);
    // topP
    Zotero?.Prefs?.set?.("extensions.zotero.selfAI.topP", "1.0", true);
    // maxTokens
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.maxTokens",
      "4096# Usage:",
      true,
    );
    // requestTimeout
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.requestTimeout",
      "15000",
      true,
    );
    // openaiApiUrl
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.openaiApiUrl",
      "https://api.openai.com/v1/responses",
      true,
    );
    // openaiApiKey
    Zotero?.Prefs?.set?.("extensions.zotero.selfAI.openaiApiKey", "", true);
    // openaiApiModel
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.openaiApiModel",
      "gpt-5",
      true,
    );
    // geminiApiUrl
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.geminiApiUrl",
      "https://generativelanguage.googleapis.com",
      true,
    );
    // geminiApiKey
    Zotero?.Prefs?.set?.("extensions.zotero.selfAI.geminiApiKey", "", true);
    // geminiModel
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.geminiModel",
      "gemini-2.5-pro",
      true,
    );
    // anthropicApiUrl
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.anthropicApiUrl",
      "https://api.anthropic.com",
      true,
    );
    // anthropicApiKey
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.anthropicApiKey",
      "",
      true,
    );
    // anthropicModel
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.anthropicModel",
      "claude-sonnet-4-5-20250929",
      true,
    );
    // openaiCompatApiUrl
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.openaiCompatApiUrl",
      "https://api.openai.com/v1/chat/completions",
      true,
    );
    // openaiCompatApiKey
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.openaiCompatApiKey",
      "",
      true,
    );
    // openaiCompatModel
    Zotero?.Prefs?.set?.(
      "extensions.zotero.selfAI.openaiCompatModel",
      "gpt-3.5-turbo",
      true,
    );
  } catch (e) {
    /* ignore in non-test env */
  }
})();
export {};
